# 要件定義書

2026年8月22日

## 概要

24時間制の時計を表示するモバイル端末向けWebアプリを作成する。

iPhone 8を優先対象としつつ、PCや他の端末でも利用可能にする。

座り過ぎの防止を目的とした作業・休憩タイマーを備える。

## 技術構成

- Vueを使用する
- TypeScriptを使用する
- PWAとして提供する
- ホーム画面への追加と、アプリ風の全画面表示に対応する
- 初回読み込み後は、通信がない状態でも時計とタイマーを利用可能にする
- 対応ブラウザはSafariとする
- iPhone 8で利用可能な最新iOSを優先対象とする

## 対応画面

- iPhone 8の縦向き画面を優先して設計する
- 縦向きと横向きの両方に対応する
- PCを含む画面サイズに応じて、時計のサイズと移動範囲を自動的に調整する

## 時計表示

- 時刻は24時間制で表示する
- 時刻、秒、日付、曜日を表示する
- 画面を常時点灯させる機能は必須としない
- 時刻を最も大きく表示し、その下に日付と曜日を小さく表示する
- 秒は時・分よりも細く、または小さく表示する
- 時刻、秒、日付、曜日を一つの表示ブロックとして扱う
- 日付と時刻は端末のローカル設定に従う
- 日付は日本語で「YYYY年M月D日（曜）」形式に表示する
- 表示フォントにはUDEV Gothicを使用する
- UDEV GothicをPWAに同梱し、オフライン時にも同じフォントで表示する

## 焼き付き防止

- 時間帯を問わず、常に焼き付き防止を行う
- DVDロゴのスクリーンセーバーのように、時計の表示ブロックを画面内でゆっくり移動させる
- 表示ブロックが画面端に当たったときは、当たった角度に応じて進行方向を反射させる
- 通常時の移動速度の初期値は15px/sとし、画面サイズや向きが変わっても速度を維持する
- 深夜表示では通常時より遅い固定速度を初期値とする
- 画面横断にかかる時間は画面サイズや向きによって変動するものとし、一定時間での横断を要件としない
- 移動速度は10・15・25px/sの3段階から選択式で変更できる
- 画面端から十分な余白を確保し、文字が欠けない範囲だけを移動に使用する
- 時計を1地点に長時間留めない
- 操作メニューや設定画面を表示している間も、背景の表示ブロックの移動を継続する
- 端末で動きを減らす設定が有効な場合は、ゆっくりした段階移動に切り替える
- 画面のサイズや向きが変わった場合は、表示位置と進行方向を安全な範囲へ補正する

## 深夜表示

- 1:00から7:00までは、暗い背景に時刻を薄く表示する
- Webアプリから端末自体の画面輝度は変更せず、画面の配色によって暗く見せる
- 画面をタップすると通常表示へ切り替える（深夜表示中はタイマー操作や操作メニューの表示を実行しない）
- 通常表示へ切り替えてから30秒経過すると、自動的に深夜表示へ戻す
- 30秒の経過前でも、画面を再度タップすると深夜表示へ戻す
- 操作メニューを閉じた場合も深夜表示へ戻す

## 配色とテーマ

- テーマはダークとライトの2種類とする
- 初期テーマはダークテーマとする
- ダークテーマは黒い背景と暖かみのある白い文字の組み合わせとする
- ライトテーマは明るい背景と読みやすい濃色の文字の組み合わせとする
- 画面端で反射するたびに時計の色を変更する演出を、ユーザーが任意で有効にできる
- 色を変更する場合は、彩度を抑えた色へゆっくり切り替える
- 深夜表示では選択中の通常テーマにかかわらず、黒い背景と暗いグレーまたは暗い赤の文字を使用する

## 時計画面の操作

- 起動直後から時計画面を表示し、初回案内や説明画面は表示しない
- 通常時の画面タップは、現在のタイマー状態に応じた主要操作（開始、一時停止、再開）に割り当てる
- 通常時の画面タップでは、操作メニューの覗き見表示や完全表示を行わない
- タッチ操作端末では、画面下端から上方向へスワイプすると操作メニューを完全に表示する
- PCなどホバー操作が可能な端末では、画面下端へのホバーで、操作メニューの上端を画面下から覗かせる
- ポインターが画面下端から離れた場合は、覗き見表示を収納する
- ポインターが画面下端に留まっている場合でも、覗き見表示から30秒経過すると自動的に収納する
- 覗き見表示中のメニューをクリックすると、操作メニューを完全に表示する
- 自動収納後に再度覗き見を表示するには、ポインターを画面下端から一度離して再度ホバーする
- 操作メニュー以外の画面をタップまたはクリックすると、表示中の操作メニューを閉じる
- 操作メニューには、現在のタイマー状態に応じて「作業開始」「一時停止」「再開」「終了」「設定」を表示する
- 操作メニューは、表示してから30秒経過すると自動的に隠す
- 操作中であっても自動収納時間は延長しない
- 操作メニューの自動収納時間は当面30秒固定とし、将来的に複数の時間または自動収納なしから選択できる設定の追加を検討する
- 操作メニューを常時表示せず、時計表示と同様に固定表示による焼き付きを防止する
- 時計画面にはタイマーの主要操作用ボタンを常時表示しない
- 時計画面の背景全体を、タップ、クリック、Enter、Spaceで操作可能な一つの主要操作領域とする
- 主要操作領域には、現在実行できる操作に応じて「作業開始」「休憩開始」「一時停止」「再開」の支援技術向けラベルを設定する
- 画面下端からのスワイプ、操作メニュー内の操作、操作メニューを閉じるための範囲外操作では、タイマーの主要操作を実行しない

### 時計画面の主要操作状態遷移

| 表示状態           | タイマー状態                 | 時計画面のタップ・クリック | 遷移結果                     |
| ------------------ | ---------------------------- | -------------------------- | ---------------------------- |
| 通常表示           | 待機中                       | 作業開始                   | 作業タイマー実行中           |
| 通常表示           | 作業・休憩タイマー実行中     | 一時停止                   | 同じフェーズの一時停止中     |
| 通常表示           | 作業・休憩タイマー一時停止中 | 再開                       | 同じフェーズのタイマー実行中 |
| 通常表示           | 作業タイマー完了             | 休憩開始                   | 休憩タイマー実行中           |
| 通常表示           | 休憩タイマー完了             | 作業開始                   | 作業タイマー実行中           |
| 深夜表示           | 任意                         | 通常表示へ復帰             | タイマー状態を変更しない     |
| 操作メニュー表示中 | 任意                         | メニュー範囲外から閉じる   | タイマー状態を変更しない     |

- 深夜表示の解除、操作メニューを閉じる操作、タイマーの主要操作を一回の入力で同時に実行しない
- タッチ端末で画面下端からのスワイプ後にclickイベントが発生しても、タイマー状態を変更しない
- タイマーの「終了」は画面全体の主要操作に割り当てず、操作メニューから実行する

## 座り過ぎ防止タイマー

- ユーザーの操作によって作業タイマーを開始する
- 作業時間の初期値は50分とする
- 休憩時間の初期値は10分とする
- 作業時間と休憩時間はユーザーが変更できる
- 作業時間の終了を通知した後、ユーザーが「休憩開始」を操作して休憩タイマーを開始する
- 休憩時間の終了を通知した後、ユーザーが「作業開始」を操作して次の作業タイマーを開始する
- 作業と休憩は自動開始せず、毎回ユーザーの操作によって開始する
- タイマーには「一時停止」「再開」「終了」の操作を用意する
- タイマー動作中は、時計の下にタイマー状態を示すアイコンと残り時間を表示する
- 時計、タイマーの状態、残り時間、日付、曜日は常に一つの表示ブロックとして扱い、別々には移動させない
- タイマー開始時は、実行中のアプリ内で終了予定時刻を保持する
- アプリがバックグラウンドになった場合は、復帰時に終了予定時刻を基準として残り時間を復元する
- 復帰時に終了予定時刻を過ぎていた場合は、タイマーの終了を表示する
- 再読み込み時は、タイマーを含むすべての実行状態をリセットする
- 一時停止時にその時点の残り時間を計算する
- 再開時は、保存していた残り時間を基準に終了予定時刻を再計算する
- タイマーの状態や終了予定時刻は端末へ保存しない
- バックグラウンド中の通知動作は保証しない
- タイマー開始時に、バックグラウンド通知に関する警告は表示しない
- タイマーには秒単位の厳密な計測精度を求めない

### タイマーの状態遷移

1. 待機画面で「作業開始」を操作する
2. 作業タイマーを開始する
3. 作業時間が終了したら通知する
4. 「休憩開始」を操作すると休憩タイマーを開始する
5. 休憩時間が終了したら通知する
6. 「作業開始」を操作すると次の作業タイマーを開始する

## タイマー通知

- 画面表示による通知を行う
- 通知音はユーザーが任意で有効にできる
- 通知音の初期設定は無効とする
- 通知音には、驚かせない短く柔らかいチャイムを使用し、1回だけ鳴らす
- 通知音を設定画面で試聴できるようにする
- 端末が消音の場合は画面表示だけで通知する
- タイマー完了時も専用の通知画面へ切り替えず、時計、完了状態、残り時間、日付、曜日を一つの表示ブロックとして移動し続ける
- タイマー完了時は残り時間を「00:00」で維持し、穏やかなオレンジ系の文字色と完了アイコンで通知する
- 完了表示は点滅させず、数秒かけて穏やかに色を変化させる
- 作業時間の完了後は、時計画面のタップ、クリック、Enter、Spaceで休憩タイマーを開始する
- 休憩時間の完了後は、時計画面のタップ、クリック、Enter、Spaceで作業タイマーを開始する
- 完了状態の主要操作領域には、次に実行できる操作に応じて「休憩開始」または「作業開始」の支援技術向けラベルを設定する

## タイマー状態の表示

- 待機中は暖かみのある白を基調とする
- 作業中は時計の文字色を落ち着いた青系にし、作業を示すアイコンを表示する
- 休憩中は時計の文字色を落ち着いた緑系にし、休憩を示すアイコンを表示する
- 一時停止中は一時停止アイコンを追加表示する
- タイマー完了時は穏やかなオレンジ系の文字色と完了アイコンで状態を示す
- 色だけに依存せず、形状の異なるアイコンでも状態を示す
- 状態アイコンには、支援技術向けの「作業」「休憩」「一時停止」「完了」ラベルを設定する
- 選択中のテーマに合わせて、各状態色を彩度の低い色へ調整する

## 設定

- 作業時間、休憩時間、通知音などの設定を、同じ端末の同じブラウザ内に保存する
- アカウント登録やサーバーへの設定保存は行わない
- 作業時間は45分、50分、60分、75分から選択し、初期値は50分とする
- 休憩時間は10分、15分、20分から選択し、初期値は10分とする
- 作業時間と休憩時間は固定候補を表示する選択式とし、自由な数値入力や「−」「＋」ボタンは使用しない
- 動作中のタイマーには設定変更を反映せず、次回のタイマー開始時から反映する
- 時計の移動速度は10・15・25px/sの3段階から選択し、初期値は15px/sとする
- 移動速度の選択肢は10px/sを「遅い」、15px/sを「標準」、25px/sを「速い」と表示する
- 設定画面は端末ごとに別の画面を作らず、共通の一つのレスポンシブなパネルとする
- スマートフォンの縦向きでは、設定パネルを画面下部にほぼ全幅の1列レイアウトで表示する
- スマートフォンの横向きや十分な幅と高さがある画面では、同じ設定パネルを最大2列のレイアウトで表示する
- PCでは設定パネルの最大幅を制限して画面中央へ表示し、利用可能な幅と高さに応じて1列または2列へ切り替える
- レイアウトの切り替えは端末種別ではなく、利用可能な画面の幅と高さを基準にする
- 設定パネルの高さが画面内に収まらない場合は、パネル内部をスクロール可能にする
- 設定パネルの背後には半透明のbackdropを表示し、背景の時計とタイマーは移動を継続する
- 設定項目は「タイマー」「サウンド」「表示」の3セクションに分ける
- 設定画面では、作業時間、休憩時間、通知音、通知音の試聴、テーマ、反射時の色変更、時計の移動速度を設定できる

## 保留事項

- 通知音の有効化、通知音の選択、試聴機能の実装は後回しとする
- 画面端で反射した際の色変更と、その有効・無効を切り替える設定の実装は後回しとする
- 設定パネルは現在の項目数ではスマートフォンとPCの縦幅に収まるため、画面幅に応じた複数カラムへの切り替えは後回しとする
- 設定項目の増加や小さい画面での表示確認により、操作性または画面内への収まりに問題が生じた段階で、設定パネルのレスポンシブレイアウトを実装する
