# 要件定義書

2026年8月22日

## 概要

24時間制の時計を表示するモバイル端末向けWebアプリを作成する。

iPhone 8を優先対象としつつ、PCや他の端末でも利用可能にする。

座り過ぎの防止を目的とした作業・休憩タイマーを備える。

## 技術構成

- Vueを使用する
- TypeScriptを使用する
- PWAとして提供する
- ホーム画面への追加と、アプリ風の全画面表示に対応する
- 初回読み込み後は、通信がない状態でも時計とタイマーを利用可能にする
- 対応ブラウザはSafariとする
- iPhone 8で利用可能な最新iOSを優先対象とする

## 対応画面

- iPhone 8の縦向き画面を優先して設計する
- 縦向きと横向きの両方に対応する
- PCを含む画面サイズに応じて、時計のサイズと移動範囲を自動的に調整する

## 時計表示

- 時刻は24時間制で表示する
- 時刻、秒、日付、曜日を表示する
- 画面を常時点灯させる機能は必須としない
- 時刻を最も大きく表示し、その下に日付と曜日を小さく表示する
- 秒は時・分よりも細く、または小さく表示する
- 時刻、秒、日付、曜日を一つの表示ブロックとして扱う
- 日付と時刻は端末のローカル設定に従う
- 日付は日本語で「YYYY年M月D日（曜）」形式に表示する
- 表示フォントにはUDEV Gothicを使用する
- UDEV GothicをPWAに同梱し、オフライン時にも同じフォントで表示する

## 焼き付き防止

- 時間帯を問わず、常に焼き付き防止を行う
- DVDロゴのスクリーンセーバーのように、時計の表示ブロックを画面内でゆっくり移動させる
- 表示ブロックが画面端に当たったときは、当たった角度に応じて進行方向を反射させる
- 通常時は約60秒で画面を横断する速度を初期値とする
- 深夜表示では約120秒で画面を横断する速度を初期値とする
- 移動速度はユーザーが変更できる
- 画面端から十分な余白を確保し、文字が欠けない範囲だけを移動に使用する
- 時計を1地点に長時間留めない
- ユーザーが操作している間は移動を一時停止する
- 端末で動きを減らす設定が有効な場合は、ゆっくりした段階移動に切り替える
- 画面のサイズや向きが変わった場合は、表示位置と進行方向を安全な範囲へ補正する

## 深夜表示

- 1:00から7:00までは、暗い背景に時刻を薄く表示する
- Webアプリから端末自体の画面輝度は変更せず、画面の配色によって暗く見せる
- 画面をタップすると通常表示へ切り替え、同時に操作メニューを表示する
- 通常表示へ切り替えてから30秒経過すると、自動的に深夜表示へ戻す
- 30秒の経過前でも、画面を再度タップすると深夜表示へ戻す
- 操作メニューを閉じた場合も深夜表示へ戻す

## 配色とテーマ

- テーマはダークとライトの2種類とする
- 初期テーマはダークテーマとする
- ダークテーマは黒い背景と暖かみのある白い文字の組み合わせとする
- ライトテーマは明るい背景と読みやすい濃色の文字の組み合わせとする
- 画面端で反射するたびに時計の色を変更する演出を、ユーザーが任意で有効にできる
- 色を変更する場合は、彩度を抑えた色へゆっくり切り替える
- 深夜表示では選択中の通常テーマにかかわらず、黒い背景と暗いグレーまたは暗い赤の文字を使用する

## 時計画面の操作

- 起動直後から時計画面を表示し、初回案内や説明画面は表示しない
- 通常時に画面をタップすると、画面下部に操作メニューを表示する
- 操作メニューには、現在のタイマー状態に応じて「作業開始」「一時停止」「再開」「終了」「設定」を表示する
- 操作メニューは、操作終了から5秒経過すると自動的に隠す
- 操作メニューを操作している間は自動的に隠さない
- 操作メニューを常時表示せず、時計表示と同様に固定表示による焼き付きを防止する

## 座り過ぎ防止タイマー

- ユーザーの操作によって作業タイマーを開始する
- 作業時間の初期値は50分とする
- 休憩時間の初期値は10分とする
- 作業時間と休憩時間はユーザーが変更できる
- 作業時間の終了を通知した後、ユーザーが「休憩開始」を操作して休憩タイマーを開始する
- 休憩時間の終了を通知した後、ユーザーが「作業開始」を操作して次の作業タイマーを開始する
- 作業と休憩は自動開始せず、毎回ユーザーの操作によって開始する
- タイマーには「一時停止」「再開」「終了」の操作を用意する
- タイマー動作中は、時計の下に「作業中」または「休憩中」とタイマーの残り時間を表示する
- 時計、タイマーの状態、残り時間、日付、曜日を一つの表示ブロックとして移動させる
- タイマー開始時は、実行中のアプリ内で終了予定時刻を保持する
- アプリがバックグラウンドになった場合は、復帰時に終了予定時刻を基準として残り時間を復元する
- 復帰時に終了予定時刻を過ぎていた場合は、タイマーの終了を表示する
- 再読み込み時は、タイマーを含むすべての実行状態をリセットする
- 一時停止時にその時点の残り時間を計算する
- 再開時は、保存していた残り時間を基準に終了予定時刻を再計算する
- タイマーの状態や終了予定時刻は端末へ保存しない
- バックグラウンド中の通知動作は保証しない
- タイマー開始時に、バックグラウンド通知に関する警告は表示しない
- タイマーには秒単位の厳密な計測精度を求めない

### タイマーの状態遷移

1. 待機画面で「作業開始」を操作する
2. 作業タイマーを開始する
3. 作業時間が終了したら通知する
4. 「休憩開始」を操作すると休憩タイマーを開始する
5. 休憩時間が終了したら通知する
6. 「作業開始」を操作すると次の作業タイマーを開始する

## タイマー通知

- 画面表示による通知を行う
- 通知音はユーザーが任意で有効にできる
- 通知音の初期設定は無効とする
- 通知音には、驚かせない短く柔らかいチャイムを使用し、1回だけ鳴らす
- 通知音を設定画面で試聴できるようにする
- 端末が消音の場合は画面表示だけで通知する
- 通知画面は点滅させず、数秒かけて穏やかに変化させる
- 作業時間が終了したら時計の移動を一時停止し、画面中央に「作業時間が終了しました」と「休憩開始」ボタンを表示する
- 休憩時間が終了したら時計の移動を一時停止し、画面中央に「休憩が終了しました」と「作業開始」ボタンを表示する
- 終了画面は、ユーザーが次の開始ボタンを操作するまで表示する
- 終了画面は画面中央に30秒間静止させ、その後は焼き付き防止のためメッセージとボタンをまとめて非常にゆっくりDVDバウンスさせる

## タイマー状態の表示

- 待機中は暖かみのある白を基調とする
- 作業中は落ち着いた青系を基調とする
- 休憩中は落ち着いた緑系を基調とする
- 終了通知は穏やかなオレンジ系を基調とする
- 色だけに依存せず、「作業中」「休憩中」などの文字でも状態を示す
- 選択中のテーマに合わせて、各状態色を彩度の低い色へ調整する

## 設定

- 作業時間、休憩時間、通知音などの設定を、同じ端末の同じブラウザ内に保存する
- アカウント登録やサーバーへの設定保存は行わない
- 作業時間は1〜180分の範囲で、1分単位で変更できる
- 休憩時間は1〜60分の範囲で、1分単位で変更できる
- 作業時間と休憩時間は数値入力および「−」「＋」ボタンで変更できる
- 動作中のタイマーには設定変更を反映せず、次回のタイマー開始時から反映する
- 設定画面は、時計画面の下から表示されるパネル形式とする
- 設定画面では、作業時間、休憩時間、通知音、通知音の試聴、テーマ、反射時の色変更、時計の移動速度を設定できる
- 設定画面を開いている間は背景の時計を停止し、設定画面を閉じると移動を再開する
